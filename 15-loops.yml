- name: loops
  hosts: localhost
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}"
    loop:
    - Ramesh
    - Suresh
    - Raheem
    - John