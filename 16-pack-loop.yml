- name: Install Packages
  hosts: localhost
  connection: local
  become: yes
  tasks:
  - name: Install packages one by one
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip
